# Zolo blog

Fully frontend app. No backend required.

Create your blog entry in the `blog/`, like `blog/init` and add a `page.mdx` to it.
Write the contents in your mdx file. The sidebar will handle it and the page will be displayed.

We have one CustomImage and Youtube component to add Image of custom size in the blog or embed a video into the blog.
