import CustomImage from '../app/components/CustomImage'

# My first entry

## A note from my personal blog

This is the content for my first post. Everything inside this wrapper is rendered inside the shared blog layout.

### Why this works

- The sidebar is shared by the parent component.
- Each MDX file is a single blog entry.
- I can write new entries in one file and deploy them anywhere.

<CustomImage src='/image.png' type='big' />